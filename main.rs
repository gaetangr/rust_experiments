fn main() {
    // ! means we are using a Macro
    // The main function is special: it is always the first code that runs in every executable Rust program.
    println!("Hello, world!"); 
    // rustc main.rs generates a binary, but does not execute it. 
}